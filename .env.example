# Supabase — copy to .env.local and fill from your project settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Used by `npm run gen:types` (Supabase CLI) — not exposed to the client.
SUPABASE_PROJECT_ID=
